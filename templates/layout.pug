doctype html
html(lang=lang)
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title= title || "Data360 Monitor"
    meta(name="description" content="Autonomous monitor that detects newsworthy events across the 12,000 indicators of Data360 and delivers them verified to LAC newsrooms.")
    link(rel="stylesheet" href="/static/css/tokens.css")
    link(rel="stylesheet" href="/static/css/main.css")
  body
    block content
    block scripts
